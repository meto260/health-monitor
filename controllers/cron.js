module.exports.ListJobs = (q, s) => {
    return s.json({status:1});
}

module.exports.AddJob = (q, s) => {
    return s.json({status:1});
}

module.exports.UpdateJob = (q, s) => {
    return s.json({status:1});
}

module.exports.RemoveJob = (q, s) => {
    return s.json({status:1});
}

